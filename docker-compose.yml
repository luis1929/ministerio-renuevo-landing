services:
  qwen:
    image: ghcr.io/qwenlm/qwen:latest
    container_name: qwen
    ports:
      - "8000:8000"
    environment:
      - MODEL_NAME=qwen2-7b-instruct
      - MODEL_PATH=/models/qwen2-7b-instruct
    volumes:
      - ./models:/models
    restart: always
